rules:
  - node: type.sequence
    mode: deterministic
    renderer: type
    bindings: [value]
    template: "Seq<{value}>"

  - node: type.optional
    mode: deterministic
    renderer: type
    bindings: [value]
    template: "Option<{value}>"
