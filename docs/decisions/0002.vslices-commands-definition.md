---
id: 0002.separate-generate-and-validate-commands
type: decision-record
status: active
level: L1
related:
  - context.vslices-tooling
  - process.up-context.document-generation
  - process.up-structure.document-generation
  - flow.use-case.template-generation.md
---

# Separación entre generación y validación

## Decisión

VSlices Tooling separará los comandos de generación y validación.

`vslices generate` tendrá como responsabilidad generar artifacts. Para hacerlo, podrá ejecutar validaciones necesarias para evitar construir desde entradas inválidas, pero no tendrá como responsabilidad principal explicar detalladamente por qué algo no es válido.

`vslices validate` tendrá como responsabilidad validar artifacts, templates o configuraciones y explicar por qué no son válidos. No construirá artifacts.

La forma general del comando de generación será:

```bash
vslices generate <target>
```

Para esta iteración, el único target soportado será:

```bash
vslices generate document
```

El target `document` representa generación documental.

## Contexto

Durante Planning se observó que la generación documental desde templates YAML necesita validar entradas antes de construir un documento Markdown.

Sin embargo, también apareció una diferencia importante entre dos necesidades:

* generar un documento cuando todo está suficientemente correcto
* diagnosticar por qué una plantilla, configuración o entrada no es válida

Ambas necesidades usan validación, pero no tienen la misma intención.

La generación busca producir un artifact.
La validación busca explicar problemas.

## Problema o tensión

La tensión principal era evitar que `vslices generate` se convirtiera en un comando demasiado amplio.

Si `generate` valida, diagnostica, explica, sugiere correcciones y además construye, el primer slice crece demasiado rápido.

También existía una tensión de diseño en la forma del CLI. El comando `generate` podría crecer en el futuro para generar distintos tipos de artifacts, no solamente documentos.

Por eso se necesitaba separar:

* la acción principal
* el target que se quiere generar
* la profundidad del diagnóstico esperado

## Razonamiento

Se escogió separar `generate` y `validate` porque representan intenciones distintas del usuario.

Cuando el usuario ejecuta `generate`, espera que VSlices Tooling construya algo. Si algo falla, basta con entregar un error esperado y detener la generación.

Cuando el usuario ejecuta `validate`, espera entender qué está mal. Ese comando puede entregar más detalle, listar múltiples errores y ayudar a corregir templates o configuraciones.

También se escogió usar `generate <target>` para permitir crecimiento progresivo sin cambiar la forma base del CLI.

En esta iteración solo se implementará:

```bash
vslices generate document --type context --level L0 --language es --output-path ./ --name name
```

Otros targets, como `path`, quedan fuera de alcance.

## Tradeoffs

| Decisión o efecto                                   | Ganancia                                                            | Costo, riesgo o pérdida                                                   |
| --------------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| Separar `generate` y `validate`.                    | Cada comando tiene una intención clara.                             | Se deberá implementar y documentar más de un comando en el futuro.        |
| `generate` valida solo lo necesario para construir. | Mantiene pequeño el primer slice.                                   | No entrega diagnóstico profundo cuando algo falla.                        |
| `validate` explicará por qué algo no es válido.     | Permite diagnóstico detallado sin mezclarlo con generación.         | Queda fuera del primer slice si no es necesario para generar.             |
| Usar `generate <target>`.                           | Permite extender generación a otros artifacts sin rediseñar el CLI. | Requiere definir targets soportados y errores para targets no soportados. |
| Soportar solo `doc` en esta iteración.              | Mantiene foco en generación documental.                             | Otros targets deberán esperar iteraciones futuras.                        |

## Consecuencias

El caso de uso de generación documental debe modelar `document` como target obligatorio para esta iteración.

`vslices generate document` debe validar que el target sea soportado antes de continuar.

Si se solicita un target no soportado, el sistema debe responder con un error esperado.

El comando `vslices validate` queda reconocido como dirección futura, pero no necesita implementarse para completar el primer slice si `generate document` puede validar lo mínimo necesario para generar.

El tooling no debe intentar entregar diagnóstico profundo desde `generate`. Esa responsabilidad pertenece a `validate`.

## Condiciones de revisión

Esta decisión debe revisitarse si:

* `generate` empieza a necesitar diagnósticos detallados para ser usable
* `validate` se vuelve necesario para completar el primer slice
* aparecen múltiples targets de generación y la forma `generate <target>` deja de ser suficiente
* los errores simples de `generate` no entregan información mínima útil
* otros productos de VSlices requieren una convención distinta de CLI
